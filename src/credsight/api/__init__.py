"""HTTP API — a thin FastAPI shell over credsight.service. Endpoints mirror the frontend
client (frontend/src/api.ts); response shapes mirror frontend/src/types.ts.

Run: `uvicorn credsight.api.app:app --reload --port 8000`  (or `credsight-api`).
"""
